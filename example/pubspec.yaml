name: overlay_layers_example
description: Example app demonstrating overlay_layers package usage
publish_to: 'none'

version: 1.0.0

environment:
  sdk: ^3.9.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  overlay_layers:
    path: ../

flutter:
  uses-material-design: true
